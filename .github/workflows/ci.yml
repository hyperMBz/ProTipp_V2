name: ProTipp V2 CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run Linting
        run: bun run lint

      - name: Build Project
        run: bun run build
        
      # A tesztelés lépését egyelőre kikommentezzük, amíg nincsenek tesztek.
      # Ha lesznek, csak ki kell venni a kommentet.
      # - name: Run Tests
      #   run: bun test
